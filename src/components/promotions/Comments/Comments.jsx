import { CommentsContainer } from "./Style";

export default function PromotionComments({ children }) {

    return (
        <CommentsContainer>
            {children}
        </CommentsContainer>
    )
}